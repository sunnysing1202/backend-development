<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>All posts</title>
        <!-- Add this in your <head> section -->
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&family=Playfair+Display:wght@400;500;600&family=Open+Sans:wght@400;500;600&display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="stylesheet" href="/style.css" />
    </head>
    <body>
         <div class="header-container">
    <h1 class="page-title text-center">Quora Posts</h1>
    <div class="title-underline"></div>
</div>
        <div class="container">
    <div class="row">
        <div class="col">


<!-- index.ejs -->
<div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
    <% if (blogs && blogs.length > 0) { %>
        <% for(const blog of blogs) { %>
            <div class="col">
                <div class="card h-100 shadow">
                    <div class="card-body">
                        <h3 class="user">@<%= blog.username %></h3>
                        <div class="content-preview">
                            <p class="content">
                                <%= blog.content.length > 100 ? blog.content.substring(0, 100) + '...' : blog.content %>
                            </p>
                        </div>
                        <div class="mt-auto button-container d-flex flex-row  flex-md-row gap-2">
                            <a class="btn btn-primary flex-fill" href="/posts/<%= blog.id %>">See In Detail</a>
                            <a class="btn btn-secondary flex-fill" href="/posts/<%= blog.id %>/edit">Edit</a>
                            <form method="post" action="/posts/<%= blog.id %>?_method=DELETE" class="flex-fill d-none d-md-block">
                                <button class="btn btn-danger w-100 d-none d-md-block">Delete</button>
                            </form>
                        </div>
<div class="mt-auto button-container d-flex flex-row  flex-md-row gap-2">

                            <form method="post" action="/posts/<%= blog.id %>?_method=DELETE" class="flex-fill d-block
                            d-md-none">
                                <button class="btn btn-danger w-100 ">Delete</button>
                            </form>
                        </div>

                    </div>
                </div>
            </div>
        <% } %>
    <% } else { %>
        <p>No posts found</p>
    <% } %>
</div>







        </div>
    </div>
        </div>
        <div class="d-flex justify-content-left mb-4 mt-5 mx-5">
    <a href="/posts/new" class="btn btn-create-simple">
        Create New Post
    </a>
</div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    </body>
    </html>
